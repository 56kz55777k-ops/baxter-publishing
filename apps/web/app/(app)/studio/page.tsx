/**
 * Authenticated creator surface — placeholder for Slice 2.
 * Real implementation lands with auth + creator profile.
 */
export default function AppHome() {
  return (
    <main className="px-gutter py-32">
      <p className="metadata">Your studio</p>
      <p className="font-serif text-lede max-w-measure mt-6">
        This is where publications begin.
      </p>
    </main>
  );
}
